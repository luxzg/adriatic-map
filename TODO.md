# TODO

Only active work belongs here.

## Release validation

- Visually test version 0.2.0 on Linux: switch all five distances, change color with
  both controls, verify invalid hex input is rejected, and confirm map clicks inspect
  points only while the inspection tool is enabled.
- Run and visually inspect the Windows binary before describing Windows runtime
  behavior as user-validated or publishing a tagged cross-platform release.
